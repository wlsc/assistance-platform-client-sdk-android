package de.tudarmstadt.informatik.tk.kraken.android.sdk.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import de.tudarmstadt.informatik.tk.kraken.android.sdk.db.SensorNetworkTraffic;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SENSOR_NETWORK_TRAFFIC".
*/
public class SensorNetworkTrafficDao extends AbstractDao<SensorNetworkTraffic, Long> {

    public static final String TABLENAME = "SENSOR_NETWORK_TRAFFIC";

    /**
     * Properties of entity SensorNetworkTraffic.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property AppName = new Property(1, String.class, "appName", false, "APP_NAME");
        public final static Property RxBytes = new Property(2, Long.class, "rxBytes", false, "RX_BYTES");
        public final static Property TxBytes = new Property(3, Long.class, "txBytes", false, "TX_BYTES");
        public final static Property Timestamp = new Property(4, Long.class, "timestamp", false, "TIMESTAMP");
        public final static Property Background = new Property(5, Boolean.class, "background", false, "BACKGROUND");
        public final static Property Longitude = new Property(6, Double.class, "longitude", false, "LONGITUDE");
        public final static Property Latitude = new Property(7, Double.class, "latitude", false, "LATITUDE");
    };


    public SensorNetworkTrafficDao(DaoConfig config) {
        super(config);
    }
    
    public SensorNetworkTrafficDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SENSOR_NETWORK_TRAFFIC\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"APP_NAME\" TEXT," + // 1: appName
                "\"RX_BYTES\" INTEGER," + // 2: rxBytes
                "\"TX_BYTES\" INTEGER," + // 3: txBytes
                "\"TIMESTAMP\" INTEGER," + // 4: timestamp
                "\"BACKGROUND\" INTEGER," + // 5: background
                "\"LONGITUDE\" REAL," + // 6: longitude
                "\"LATITUDE\" REAL);"); // 7: latitude
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SENSOR_NETWORK_TRAFFIC\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SensorNetworkTraffic entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String appName = entity.getAppName();
        if (appName != null) {
            stmt.bindString(2, appName);
        }
 
        Long rxBytes = entity.getRxBytes();
        if (rxBytes != null) {
            stmt.bindLong(3, rxBytes);
        }
 
        Long txBytes = entity.getTxBytes();
        if (txBytes != null) {
            stmt.bindLong(4, txBytes);
        }
 
        Long timestamp = entity.getTimestamp();
        if (timestamp != null) {
            stmt.bindLong(5, timestamp);
        }
 
        Boolean background = entity.getBackground();
        if (background != null) {
            stmt.bindLong(6, background ? 1L: 0L);
        }
 
        Double longitude = entity.getLongitude();
        if (longitude != null) {
            stmt.bindDouble(7, longitude);
        }
 
        Double latitude = entity.getLatitude();
        if (latitude != null) {
            stmt.bindDouble(8, latitude);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SensorNetworkTraffic readEntity(Cursor cursor, int offset) {
        SensorNetworkTraffic entity = new SensorNetworkTraffic( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // appName
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // rxBytes
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // txBytes
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // timestamp
            cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0, // background
            cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6), // longitude
            cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7) // latitude
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SensorNetworkTraffic entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAppName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setRxBytes(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setTxBytes(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setTimestamp(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setBackground(cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0);
        entity.setLongitude(cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6));
        entity.setLatitude(cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SensorNetworkTraffic entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SensorNetworkTraffic entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
