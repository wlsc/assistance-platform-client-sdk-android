package de.tudarmstadt.informatik.tk.kraken.android.sdk.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import de.tudarmstadt.informatik.tk.kraken.android.sdk.db.SensorRunningTasks;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table SENSOR_RUNNING_TASKS.
*/
public class SensorRunningTasksDao extends AbstractDao<SensorRunningTasks, Long> {

    public static final String TABLENAME = "SENSOR_RUNNING_TASKS";

    /**
     * Properties of entity SensorRunningTasks.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property RunningTasks = new Property(1, String.class, "runningTasks", false, "RUNNING_TASKS");
        public final static Property StackPosition = new Property(2, Integer.class, "stackPosition", false, "STACK_POSITION");
        public final static Property Timestamp = new Property(3, Long.class, "timestamp", false, "TIMESTAMP");
    };


    public SensorRunningTasksDao(DaoConfig config) {
        super(config);
    }
    
    public SensorRunningTasksDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'SENSOR_RUNNING_TASKS' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'RUNNING_TASKS' TEXT," + // 1: runningTasks
                "'STACK_POSITION' INTEGER," + // 2: stackPosition
                "'TIMESTAMP' INTEGER);"); // 3: timestamp
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'SENSOR_RUNNING_TASKS'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SensorRunningTasks entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String runningTasks = entity.getRunningTasks();
        if (runningTasks != null) {
            stmt.bindString(2, runningTasks);
        }
 
        Integer stackPosition = entity.getStackPosition();
        if (stackPosition != null) {
            stmt.bindLong(3, stackPosition);
        }
 
        Long timestamp = entity.getTimestamp();
        if (timestamp != null) {
            stmt.bindLong(4, timestamp);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SensorRunningTasks readEntity(Cursor cursor, int offset) {
        SensorRunningTasks entity = new SensorRunningTasks( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // runningTasks
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // stackPosition
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3) // timestamp
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SensorRunningTasks entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setRunningTasks(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setStackPosition(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setTimestamp(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SensorRunningTasks entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SensorRunningTasks entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
